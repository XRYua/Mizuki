<section class="three-line-entry card-base mb-3 onload-animation">
	<div class="entry-body">
		<p class="entry-tag">✧ 活动小笺</p>
		<h2>三行情书写</h2>
		<p>把心事写成三行，落在温柔里。</p>
	</div>
	<a class="entry-btn" href="/three-line-poem/">去写一封</a>
</section>

<style>
	.three-line-entry {
		display: flex;
		justify-content: space-between;
		align-items: center;
		gap: 0.7rem;
		padding: 0.72rem 0.88rem;
		border: 1px solid color-mix(in oklab, var(--primary) 22%, transparent);
		background: linear-gradient(120deg, color-mix(in oklab, var(--primary) 6%, var(--card-bg)), var(--card-bg));
	}
	.entry-tag {
		margin: 0;
		font-size: 0.74rem;
		font-weight: 700;
		color: var(--primary);
	}
	.entry-body h2 {
		margin: 0.1rem 0;
		font-size: 0.95rem;
	}
	.entry-body p {
		margin: 0;
		font-size: 0.82rem;
		opacity: 0.88;
	}
	.entry-btn {
		white-space: nowrap;
		padding: 0.32rem 0.72rem;
		border-radius: 999px;
		border: 1px solid color-mix(in oklab, var(--primary) 28%, transparent);
		background: color-mix(in oklab, var(--primary) 10%, transparent);
		color: var(--primary);
		font-size: 0.82rem;
		font-weight: 700;
		text-decoration: none;
	}
	@media (max-width: 768px) {
		.three-line-entry {
			flex-direction: row;
			align-items: center;
		}
	}
</style>
